# 太阳系三维模拟程序规格说明（Spec）

## 1. 文档目标

定义一个可在浏览器运行的三维太阳系模拟程序需求规格，确保实现包含以下核心能力：

- 太阳发光效果
- 启动时根据当前时间初始化行星与卫星位置
- 8 大行星公转与自转
- 多个代表性卫星公转
- 土星、天王星环
- 星空背景与轨道线
- 交互控制（旋转、缩放、平移）

## 2. 适用范围

- 平台：现代桌面浏览器（Chrome / Edge / Firefox 新版本）
- 渲染：WebGL（建议基于 Three.js）
- 运行方式：静态页面即可运行（本地文件或本地静态服务）

## 3. 术语定义

- `公转`：天体绕其中心天体沿轨道运动。
- `自转`：天体绕自身轴线旋转。
- `轨道周期`：完成一圈公转所需时间。
- `初始化时刻`：程序启动时读取的当前系统时间（本地时间转 UTC 使用）。

## 4. 功能性需求

### 4.1 场景与渲染

- 必须构建 3D 场景，包含相机、光照、渲染器。
- 必须展示太阳系主视图，默认可观察到内外行星轨道分布。
- 必须支持实时动画循环（requestAnimationFrame）。

### 4.2 太阳发光效果

- 太阳必须具有明显发光视觉特征，至少包含以下之一：
  - 高发光材质（emissive）
  - 点光源（PointLight）从太阳位置向外照明
  - 可选外层光晕（glow/corona）
- 太阳应作为场景主要光源，影响行星明暗变化。

### 4.3 行星系统（8 大行星）

- 必须包含：水星、金星、地球、火星、木星、土星、天王星、海王星。
- 每颗行星必须具备：
  - 半径参数（可缩放）
  - 公转半径参数（可缩放）
  - 公转周期参数
  - 自转周期参数（允许逆行，如金星）
- 动画中必须同时体现公转与自转。

### 4.4 卫星系统（代表性）

- 必须至少实现以下代表性卫星中的多个（建议全实现）：
  - 地球：月球
  - 火星：火卫一、火卫二
  - 木星：木卫一、木卫二、木卫三、木卫四
  - 土星：土卫六
  - 天王星：至少 1 颗代表卫星
  - 海王星：海卫一
- 每颗卫星必须围绕其所属行星公转。

### 4.5 行星环

- 土星必须显示行星环。
- 天王星必须显示行星环。
- 行星环应与对应行星的赤道面一致（考虑轴倾角）。

### 4.6 星空背景与轨道线

- 背景必须有星空效果（粒子点或贴图）。
- 每颗行星必须显示轨道线。
- 卫星轨道线可选，但建议实现以增强可视化。

### 4.7 启动时按当前时间初始化位置

- 程序启动时必须读取当前系统时间，并据此初始化天体相位角（而非固定从同一角度开始）。
- 计算建议：
  - 选定参考历元 `T0`（如 J2000：2000-01-01T12:00:00Z）。
  - 计算 `Δt = nowUTC - T0`（单位：天）。
  - 行星初始角度：`theta_planet = 2π * frac(Δt / orbitalPeriodDays + phaseOffset)`。
  - 卫星初始角度：`theta_moon = 2π * frac(Δt / moonOrbitDays + phaseOffsetMoon)`。
- `phaseOffset` 可使用固定常量近似，以保证每次启动同一时刻结果一致。

### 4.8 交互控制

- 必须支持鼠标左键旋转视角。
- 必须支持滚轮缩放。
- 必须支持鼠标右键平移。
- 建议使用轨道相机控制器（如 OrbitControls）。

## 5. 非功能性需求

### 5.1 性能

- 在普通消费级电脑上应保持流畅（目标 30 FPS 以上，推荐 60 FPS）。
- 动画循环不得出现明显卡顿或内存持续增长。

### 5.2 兼容性

- 应兼容支持 ES Modules 与 WebGL 的现代浏览器。
- 在不支持 WebGL 时应给出明确提示信息。

### 5.3 可维护性

- 天体参数（半径、周期、轨道半径、颜色等）应集中配置（如 JSON/数组对象）。
- 公转、自转、初始化计算应模块化，避免写死在渲染循环中。

## 6. 建议数据模型

```ts
type CelestialBody = {
  name: string;
  radius: number;
  orbitRadius?: number;      // 对太阳或行星的轨道半径
  orbitDays?: number;        // 公转周期（天）
  rotationHours?: number;    // 自转周期（小时）
  axialTiltDeg?: number;     // 自转轴倾角
  color?: number;
  ring?: { inner: number; outer: number; color: number };
  moons?: CelestialBody[];
  phaseOffset?: number;      // 初始化相位偏移
};
```

## 7. 运行与配置要求

- 代码应可通过本地静态服务器直接运行。
- 允许通过参数调整时间倍率（例如 `speedFactor`）。
- 推荐提供暂停/继续与速度调整能力（可选增强项）。

## 8. 验收标准（Acceptance Criteria）

- [ ] 页面加载后可见完整太阳系场景，而非黑屏。
- [ ] 可观察到太阳发光效果与受光照影响的行星明暗面。
- [ ] 8 大行星均可见并持续公转与自转。
- [ ] 至少 6 组代表性卫星系统可见并正常公转。
- [ ] 土星与天王星行星环可见且姿态正确。
- [ ] 星空背景与行星轨道线清晰可见。
- [ ] 鼠标旋转、缩放、平移均可正常操作。
- [ ] 重启程序后，天体初始位置会随当前时间变化（同一时刻结果一致）。

## 9. 可选增强项（非本次必须）

- 行星点击聚焦视角与返回默认视角。
- 行星表面纹理、凹凸贴图与云层动画。
- 更精确天体历表（开普勒轨道参数）替代圆轨道近似。
- UI 控制面板（速度、轨道线开关、标签开关）。
